bafkreiehe742yroj73474igxfwrataxzun75dzczrm3gld34kw4errng6i

# CIDv1 (raw codec, sha2-256) of the file docs/agent-card.json.
# agentURI used at ERC-8004 registration:
#   ipfs://bafkreiehe742yroj73474igxfwrataxzun75dzczrm3gld34kw4errng6i
#
# Integrity verification (anyone can recompute it from the file content):
#   node scripts/ipfs-cid.mjs docs/agent-card.json
#
# NOTE (honestly): the file is NOT pinned to public IPFS (no pinning credentials
# in this environment). The CID is genuine and content-addressed, so it confirms
# the integrity of the committed content; actually serving it via a public IPFS
# gateway is a separate hosting step (Pinata/web3.storage/your own node).
